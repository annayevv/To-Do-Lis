import 'package:flutter/material.dart';

class Oraz extends StatelessWidget {
  const Oraz({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Image.asset('assets/download.png'),
      ),
    );
  }
}